# Feature — Benefits Demo

**Status:** 📋 Planned (0%) · **Release:** 2 (Depth) · **Spec:** `wiki/bridge-benefits-demo.md`, `wiki/bridge-patterns.md`

The depth example — the full arc "one comparable view": fan-out → normalize → compare/standard-gap → negotiate → bind. Program context over N isolated legs.

## Scope
- Programs (isolated legs), program-scale Gateway addressing
- Multilingual extraction + FX normalization; the benefits-quote pack
- Program-view comparison + declared-rule standard-gap
- Negotiate flow + versioned revise loop
- Per-leg bind → program rollup → audit
- **Simulated provider agents (interoperability proof)** — two Path-A carrier agents built in *different* frameworks (Carrier A = Google ADK, Carrier B = LangGraph), each a real A2A client responding to the Bridge's A2A edge from the external zone. The LangGraph carrier drives the multi-turn negotiate/revise loop, proving A2A is framework-agnostic (the ADK Bridge negotiates with a non-ADK counterparty). Demo furniture, not core.
- One live doctype-add here is the early down-payment on the RFP live-add

## Validation
Manual test: 4 carriers (2 Path-A agents ADK+LangGraph, 2 Path-B PDFs), 3 currencies/languages, a live negotiate round with the LangGraph carrier, bind → rollup. Cross-framework interop test: same A2A conversation passes for either counterparty build.

## Completed Work
_None yet — pre-implementation._
