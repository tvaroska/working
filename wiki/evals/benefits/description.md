---
type: eval
related:
  - "[[bridge-benefits-demo]]"
tags: [bridge, eval, scaffold]
status: draft
updated: 2026-08-15
---

# Eval — Benefits *(scaffold — not yet authored)*

> **Scaffold only.** Benefits is the Phase-2 (aspirational) demo; its eval corpus and expected outputs are **not yet authored**. This page fixes the structure so the eval can be filled in when the demo is built. **Evals are not skills** — runtime config will live in `skills/` when authored.

## Use-case *(to be written)*

The Negotiate arc — fan-out to multiple carriers, normalize (FX, multilingual), compare, negotiate/revise, bind, program rollup. See [[bridge-benefits-demo]] for the design narrative.

## Example inputs + expected outputs *(to be authored)*

_No corpus yet._ When authored, each case = an input document/response (`images/…`) with fixed signals → expected disposition/gate/negotiation outcome, mirroring the [[address/description|Address eval]] shape. Populate `expected.json` and this table together.

| id (image) | doctype | signals | **expected outcome** | what it proves |
|---|---|---|---|---|
| _tbd_ | _tbd_ | _tbd_ | _tbd_ | _tbd_ |

## Files

- **`expected.json`** — placeholder (`documents: []`); fill with the eval corpus.
- **`images/`** — empty; add synthetic, no-PII example documents.

## Related
- [[bridge-benefits-demo|Benefits demo]], [[evals|evals index]]
