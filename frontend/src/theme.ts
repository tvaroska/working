import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  // Default palette is fine for Sprint 0
  // Sprint 1 will consolidate this into a shared theme package
});

export default theme;
