# Hoist all deps to the workspace-root node_modules so every package + app resolves
# the shared toolchain (react, mui, vite, vitest) without redeclaring it 6×. The
# workspace:* dep on @bridge/shared is still symlinked. Keeps offline install simple.
shamefully-hoist=true
